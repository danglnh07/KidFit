@using KidFit.ViewModels
@using X.PagedList
@model IPagedList<CardCategoryViewModel>
@{
    var totalPages = (int)Math.Ceiling((double)Model.TotalItemCount / Model.PageSize);
}

<div class="overflow-x-auto">
    <table class="w-full text-left border-collapse">
        <thead>
            <tr class="bg-slate-50 dark:bg-slate-800/50 border-b border-slate-200 dark:border-slate-700">
                <th class="px-6 py-4 text-xs font-bold uppercase tracking-wider text-slate-500 dark:text-slate-400">
                    Name
                </th>
                <th class="px-6 py-4 text-xs font-bold uppercase tracking-wider text-slate-500 dark:text-slate-400">
                    Description
                </th>
                <th    class="px-6 py-4 text-xs font-bold uppercase tracking-wider text-slate-500 dark:text-slate-400 hidden lg:table-cell">
                    Border Color
                </th>
                <th    class="px-6 py-4 text-xs font-bold uppercase tracking-wider text-slate-500 dark:text-slate-400 text-right">
                    Actions
                </th>
            </tr>
        </thead>
        <tbody class="divide-y divide-slate-200 dark:divide-slate-700">
            @foreach (var category in Model)
            {
                <tr class="hover:bg-slate-50 dark:hover:bg-slate-800/50 transition-colors group">
                    <td class="px-6 py-4 whitespace-nowrap text-sm text-slate-600 dark:text-slate-400">
                        @category.Name
                    </td>
                    <td class="px-6 py-4 whitespace-nowrap text-sm text-slate-600 dark:text-slate-400">
                        @category.Description
                    </td>
                    <td class="px-6 py-4 whitespace-nowrap text-sm text-slate-600 dark:text-slate-400">
                        <div class="flex items-center gap-2">
                            <span>@category.BorderColor</span>
                            <div class="w-4 h-4 rounded-full" style="background-color: @category.BorderColor">
                            </div>
                        </div>
                    </td>
                    <td class="px-6 py-4 whitespace-nowrap text-right">
                        <div class="flex items-center justify-end gap-2">
                            <a asp-action="Update" asp-controller="CardCategory" asp-route-id="@category.Id"
                                   class="p-2 text-slate-400 hover:text-primary hover:bg-primary/10 rounded-lg transition-all"
                                   title="Edit Account">
                                <span class="material-symbols-outlined text-xl">edit</span>
                            </a>
                            <form asp-action="Delete" asp-controller="CardCategory" asp-route-id="@category.Id"
                                  class="p-2 text-slate-400 hover:text-primary hover:bg-primary/10 rounded-lg transition-all">
                                <button type="submit"
                                        class="p-2 text-slate-400 hover:text-rose-500 hover:bg-rose-50 dark:hover:bg-rose-900/30 rounded-lg transition-all"
                                        title="Delete card category"
                                        onclick="return confirm('Are you sure you want to delete this card category?')">
                                    <span class="material-symbols-outlined text-xl">delete</span>
                                </button>
                            </form>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

@if (totalPages > 1)
{
    <div
        class="px-6 py-4 bg-slate-50/50 dark:bg-slate-800/30 border-t border-slate-200 dark:border-slate-700 flex items-center justify-between">
        @{
            var prevPage = Model.PageNumber > 1 ? Model.PageNumber - 1 : 1;
            var nextPage = Model.PageNumber < totalPages ? Model.PageNumber + 1 : totalPages;
        }
        @if (Model.PageNumber > 1)
        {
            <button hx-get="/CardCategory?page=@prevPage&size=@Model.PageSize" hx-target="#card_category_table" hx-swap="innerHTML"
                class="flex items-center gap-1 text-sm font-semibold text-slate-600 dark:text-slate-400 hover:text-primary transition-colors">
                <span class="material-symbols-outlined text-lg">chevron_left</span>
                Previous
            </button>
        }
        else
        {
            <span class="flex items-center gap-1 text-sm font-semibold text-slate-400 cursor-not-allowed">
                <span class="material-symbols-outlined text-lg">chevron_left</span>
                Previous
            </span>
        }

        <div class="flex items-center gap-2">
            @for (int i = 1; i <= totalPages && i <= 5; i++)
            {
                if (i == Model.PageNumber)
                {
                    <span class="h-8 w-8 flex items-center justify-center rounded-lg bg-primary text-white text-sm font-bold shadow-sm shadow-primary/20">@i</span>
                }
                else
                {
                    <button hx-get="/CardCategory?page=@i&size=@Model.PageSize"
                        hx-target="#card_category_table"
                        hx-swap="innerHTML"
                        class="h-8 w-8 flex items-center justify-center rounded-lg text-slate-500 hover:bg-primary/10 hover:text-primary text-sm font-bold transition-all">
                        @i
                    </button>
                }
            }
            @if (totalPages > 5)
            {
                <span class="text-slate-400 px-1">...</span>
                <button hx-get="/CardCategory?page=@totalPages&size=@Model.PageSize"
                    hx-target="#card_category_table"
                    hx-swap="innerHTML"
                    class="h-8 w-8 flex items-center justify-center rounded-lg text-slate-500 hover:bg-primary/10 hover:text-primary text-sm font-bold transition-all">
                    @totalPages
                </button>
            }
        </div>

        @if (Model.PageNumber < totalPages)
        {
            <button hx-get="/CardCategory?page=@nextPage&size=@Model.PageSize" hx-target="#card_category_table" hx-swap="innerHTML"
                class="flex items-center gap-1 text-sm font-semibold text-slate-600 dark:text-slate-400 hover:text-primary transition-colors">
                <span class="material-symbols-outlined text-lg">chevron_right</span>
                Next 
            </button>
        }
        else
        {
            <span class="flex items-center gap-1 text-sm font-semibold text-slate-400 cursor-not-allowed">
                Next
                <span class="material-symbols-outlined text-lg">chevron_right</span>
            </span>
        }
    </div>
}